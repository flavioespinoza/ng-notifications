'use strict';

describe('myApp.notifications module', function() {

  beforeEach(module('myApp.notifications'));

  describe('notifications controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var notificationsCtrl = $controller('notificationsCtrl');
      expect(notificationsCtrl).toBeDefined();
    }));

  });
});